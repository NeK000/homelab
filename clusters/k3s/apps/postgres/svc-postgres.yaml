apiVersion: v1
kind: Service
metadata:
  name: shared-postgres-external
  namespace: postgres
spec:
  type: LoadBalancer
  selector:
    app: shared-postgres
  ports:
    - port: 5432
      targetPort: 5432