services:
  homepage:
    image: ghcr.io/gethomepage/homepage:v1.11.0
    labels:
      traefik.enable: "true"
      traefik.http.routers.localhomepage.rule: "Host(`homepage.local`)"
      traefik.http.routers.localhomepage.entrypoints: "web"
      traefik.http.services.localhomepage.loadbalancer.server.port: "3000"
      tsdproxy.enable: "false"
    networks:
      - traefik-public
    environment:
      PUID: 1000
      PGID: 1000
      HOMEPAGE_ALLOWED_HOSTS: "homepage.local"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./homepage:/app/config:rw"
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure

  kitchen-dash:
    image: ghcr.io/gethomepage/homepage:v1.11.0
    labels:
      traefik.enable: "true"
      traefik.http.routers.kitchendash.rule: "Host(`kitchen-dash.local`)"
      traefik.http.routers.kitchendash.entrypoints: "web"
      traefik.http.services.kitchendash.loadbalancer.server.port: "3000"
      tsdproxy.enable: "false"
    networks:
      - traefik-public
    environment:
      PUID: 1000
      PGID: 1000
      HOMEPAGE_ALLOWED_HOSTS: "kitchen-dash.local"
    volumes:
      - "./kitchen-dash:/app/config:rw"
    deploy:
      restart_policy:
        condition: on-failure

networks:
  traefik-public:
    external: true
    name: traefik-public