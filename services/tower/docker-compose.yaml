# Generated by ironicbadger.docker-compose-generator
# badger badger badger mushroom mushrooooom...

services:
  hass-unraid:
    image: ghcr.io/idmedia/hass-unraid:2.1.5
    container_name: hass-unraid
    restart: unless-stopped
    network_mode: bridge
    pid: host
    deploy:
      resources:
        limits:
          pids: 2048
    environment:
      - TZ=Europe/Paris
      - HOST_OS=Unraid
      - HOST_HOSTNAME=Tower
      - HOST_CONTAINERNAME=hass-unraid
    labels:
      - net.unraid.docker.managed=dockerman
      - net.unraid.docker.icon=https://raw.githubusercontent.com/IDmedia/hass-unraid/main/extras/logo.png
    volumes:
      - "${PATH_APPDATA_UNRAID}/hass-unraid:/data:rw"
  duplicati:
    container_name: duplicati
    restart: unless-stopped
    image: duplicati/duplicati:2.2.0.103
    labels:
      tsdproxy.enable: "true"
      tsdproxy.name: "duplicati"
      tsdproxy.ephemeral: "true"
      net.unraid.docker.managed: dockerman
      net.unraid.docker.webui: http://[IP]:[PORT:8200]
      net.unraid.docker.icon: https://raw.githubusercontent.com/linuxserver/docker-templates/master/linuxserver.io/img/duplicati-logo.png
    environment:
      SETTINGS_ENCRYPTION_KEY: "${DUPLICATI_SETTINGS_ENCRYPTION_KEY}"
      DUPLICATI__WEBSERVICE_PASSWORD: "${DUPLICATI__WEBSERVICE_PASSWORD}"
    volumes:
      - "${PATH_APPDATA_UNRAID}/duplicati-config:/data"
      - "${PATH_APPDATA_UNRAID}:/mnt"
    ports:
      - "8200:8200"
  dozzle:
    image: amir20/dozzle:v9.0
    container_name: dozzle
    restart: unless-stopped
    labels:
      tsdproxy.enable: "true"
      tsdproxy.name: "dozzle"
      tsdproxy.ephemeral: "true"
      tsdproxy.container_port: "38080"
      net.unraid.docker.managed: dockerman
      net.unraid.docker.webui: http://[IP]:[PORT:38080]
      net.unraid.docker.icon: https://raw.githubusercontent.com/selfhosters/unRAID-CA-templates/master/templates/img/dozzle.png
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "38080:8080"
  kitchen-dash:
    image: ghcr.io/gethomepage/homepage:v1.10.1
    container_name: kitchen-dash
    restart: unless-stopped
    labels:
      net.unraid.docker.managed: dockerman
      net.unraid.docker.webui: http://[IP]:[PORT:3004]
      net.unraid.docker.icon: https://raw.githubusercontent.com/gethomepage/homepage/main/public/android-chrome-512x512.png
    environment:
      - PUID=1000
      - PGID=1000
      - HOMEPAGE_ALLOWED_HOSTS=kitchen-dash.local
    volumes:
      - "${PATH_APPDATA_UNRAID}/kitchen-dash:/app/config:rw"
    ports:
      - "3004:3000"
  homepage:
    image: ghcr.io/gethomepage/homepage:v1.10.1
    container_name: homepage
    restart: unless-stopped
    labels:
      tsdproxy.enable: "true"
      tsdproxy.name: "home"
      tsdproxy.ephemeral: "true"
      tsdproxy.container_port: "3005"
      net.unraid.docker.managed: dockerman
      net.unraid.docker.webui: http://[IP]:[PORT:3005]
      net.unraid.docker.icon: https://raw.githubusercontent.com/gethomepage/homepage/main/public/android-chrome-512x512.png
    environment:
      - PUID=1000
      - PGID=1000
      - HOMEPAGE_ALLOWED_HOSTS=${TAILSCALE_HOMEPAGE_URL}
    volumes:
      - "${PATH_APPDATA_UNRAID}/homepage:/app/config:rw"
    ports:
      - "3005:3000"
